name: Publish site

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: publish-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      # The rule that turns auditor opinions into a published verdict. A silent
      # change here would move verdicts on every entry at once, so it is pinned
      # by golden cases and checked before anything is built.
      - name: Adjudication rule tests
        run: node tests/adjudicate.test.mjs

      # Known-answer cases verified by hand. An audit configuration that cannot
      # reproduce them is miscalibrated, and its other verdicts are not
      # trustworthy either.
      - name: Calibration fixtures
        run: node tools/check-calibration.mjs

      # Fails in seconds on a bad verdict value or a dangling cross-link,
      # rather than after a full render.
      - name: Validate the problem registry
        run: node tools/check-registry.mjs

      - name: Set up Quarto
        # Pinned so a Quarto release cannot silently change listing behavior.
        uses: quarto-dev/quarto-actions/setup@v2
        with:
          version: 1.8.26

      - name: Render site
        uses: quarto-dev/quarto-actions/render@v2
        with:
          to: html

      # Pull requests get the build check without deploying.
      - name: Publish to GitHub Pages
        if: github.event_name != 'pull_request'
        uses: quarto-dev/quarto-actions/publish@v2
        with:
          target: gh-pages
          render: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
